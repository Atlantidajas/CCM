<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/signOutButtonToolbar"
        android:icon="@drawable/ic_action_arrow_downward"
        android:title="@string/signOutButtonToolbar_title"
        app:showAsAction="always"/>
    <item
        android:id="@+id/disconnectButton"
        android:icon="@drawable/ic_action_arrow_forward_ios_red"
        android:title="@string/disconnectButton_title"
        app:showAsAction="always"/>
</menu>